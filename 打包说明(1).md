以下为完整版发给学生的说明，整合所有要点：

---

**关于预测打榜系统测试提交的说明**

同学们好！

预测打榜系统目前尚未开放自主上传，需要先收集一批模型进行测试。请大家按以下要求打包，通过**邮箱**发送至 yihengshen@shu.edu.cn ，本次提交成绩视同正式提交，但不强制要求。

---

**一、环境说明**
评测环境已预装以下主要库（仅列出主动安装部分，依赖包未列出）：
```
numpy==1.26.4
scipy==1.13
pandas==2.2.2
scikit-learn==1.4.2
matplotlib==3.8.4
pillow==10.3.0
pytorch==2.5.1
torchvision==0.20.1
torchaudio==2.5.1
pytorch-cuda==12.4
```
本次测试仅限使用已列出的库，暂不支持第三方库（如catboost、xgboost等），模型不必在此环境中训练，只需保证推理可用。

---

**二、提交文件要求**
1. **必须使用 ZIP 格式**压缩（其他格式无法自动安全检查）。
2. 压缩包内需包含 `predict.py` 脚本及模型参数文件。
3. `predict.py` 可位于压缩包根目录，也可在**一层文件夹**内（如解压后为一个文件夹，里面直接放脚本和模型文件）。
4. 压缩包大小 ≤ **200 MB**，解压后总大小 ≤ **500 MB**。
5. 请勿在文件名中使用 `..` 等路径逃逸符号，此类文件会被系统自动排除。

---

**三、关于 predict.py 脚本**
系统将以以下命令运行脚本：
```bash
python3 predict.py --testdata /testdata --output /results/submission.csv
```
请参照先前发布的示例模板编写，确保最终生成符合格式的 `submission.csv`。如遇实现困难，可借助大语言模型辅助生成。

---

**四、特别提醒**
- 每次评测前系统会随机打乱测试集顺序，无法通过多次提交进行二分嗅探，请勿浪费算力。
- 压缩包名称任意，系统会自动重命名。

请将打包好的 zip 文件通过邮箱发送至 yihengshen@shu.edu.cn ，邮件标题和压缩包文件名建议包含学号和姓名。感谢配合！